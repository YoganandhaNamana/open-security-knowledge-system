---
id: "{{ metadata.id }}"
title: "{{ metadata.title }}"
volume: "{{ metadata.volume }}"
chapter: "{{ metadata.chapter }}"
version: "{{ metadata.version }}"
stability: "{{ metadata.stability }}"
status: "{{ metadata.status }}"

taxonomy:
  domain: "{{ metadata.taxonomy.domain }}"
  discipline: "{{ metadata.taxonomy.discipline }}"
  technology: "{{ metadata.taxonomy.technology }}"
  skill_level: "{{ metadata.taxonomy.skill_level }}"

learning_outcomes:
{% for outcome in metadata.learning_outcomes %}
  - "{{ outcome }}"
{% endfor %}

evidence:
  sources:
{% for source in metadata.evidence.sources %}
    - id: "{{ source.id }}"
      type: "{{ source.type }}"
      authority: "{{ source.authority }}"
{% endfor %}

knowledge_graph:
  prerequisites: {{ metadata.knowledge_graph.prerequisites | tojson }}
  next_topics: {{ metadata.knowledge_graph.next_topics | tojson }}
  lab_references: {{ metadata.knowledge_graph.lab_references | tojson }}
  glossary_terms: {{ metadata.knowledge_graph.glossary_terms | tojson }}
  mitre_attack: {{ metadata.knowledge_graph.mitre_attack | tojson }}
---

# {{ metadata.id }}: {{ metadata.title }}

## 📌 1. Module Overview & Domain Mechanics
> Scope, threat landscape relevance, and protocol/system operational context.

---

## 🎯 2. Prerequisites & Target Learning Outcomes
### Prerequisites
{% for prereq in metadata.knowledge_graph.prerequisites %}
* [{{ prereq }}](../{{ prereq }}/README.md)
{% else %}
* None (Foundational Module)
{% endfor %}

### Learning Objectives
{% for outcome in metadata.learning_outcomes %}
- [ ] {{ outcome }}
{% endfor %}

---

## 🧠 3. Technical Deep-Dive & Architecture
*(Populated from verified research dossier)*

---

## ⚡ 4. Operational Commands & Tooling
*(See companion document [commands.md](commands.md) for verified syntax)*

---

## 🧪 5. Hands-on Lab Mapping
{% for lab in metadata.knowledge_graph.lab_references %}
* **Associated Lab Setup**: [{{ lab }}](../../../labs/{{ lab }}.md)
{% else %}
* Pending Lab Assignment
{% endfor %}

---

## 🛡️ 6. Enterprise Defense & Hardening
* **Offensive Mechanics**: How adversaries abuse or exploit this target.
* **Defensive Controls**: Hardening steps and detection signatures.

---

## 📚 7. Reference & Citation Matrix
{% for source in metadata.evidence.sources %}
* **[{{ source.authority }} Authority]** `{{ source.id }}` ({{ source.type }})
{% endfor %}
